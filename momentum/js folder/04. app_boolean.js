// boolean

// const amIFat = null;
                // true
                // false
                // null : 아무것도 없다. , 값이 없다는 것을 의도적으로 확정하는 것.
let something;  // undefine 형식으로 출력된다.
                // 값이 정의되지 않음.

// console.log(amIFat);
console.log(something)